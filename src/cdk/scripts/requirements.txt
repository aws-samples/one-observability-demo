# CDK Scripts Python Dependencies
# Required for manage-exports.py script

# AWS SDK for Python. 1.39 is the floor, not a preference: tdir-seed-scenarios.py and
# tdir-reset.py use the bedrock-agentcore-control client, which older botocore does not
# ship a service model for and which therefore fails with UnknownServiceError.
boto3>=1.39.0

# Template engine for HTML dashboard generation
jinja2>=3.1.0

# Environment variable loading from .env files
# Matches the pattern used by CDK TypeScript code
python-dotenv>=1.0.0

# Additional dependencies that might be useful for future scripts
# JSON handling is built into Python 3
# argparse is built into Python 3
# logging is built into Python 3
# datetime is built into Python 3
