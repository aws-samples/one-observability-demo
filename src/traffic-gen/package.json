{
  "name": "petsite-canary-standalone",
  "version": "1.0.0",
  "description": "Standalone canary deployment for petsite monitoring",
  "main": "canary.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.212.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
